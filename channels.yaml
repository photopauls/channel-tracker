# List every channel you want tracked here.
#
# Just paste the channel's URL — any format works: /@handle, /@handle/videos,
# /channel/UC..., or an old /c/Name or /user/Name link. The script resolves it
# to the channel's real ID automatically on first run and caches the result,
# so you never have to go hunting for channel IDs by hand.
#
# - url: the channel URL, pasted as-is
# - niche: your own grouping label (used for the dashboard filter and for
#     spotting cross-niche patterns) - keep these consistent, e.g. always
#     "hairloss" not sometimes "hair-loss"
# - name: optional — if you leave it out, the script fills it in from
#     YouTube automatically the first time it resolves the channel
# - since: optional — defaults to the last 90 days. Set this to a date
#     ("YYYY-MM-DD") to backfill that channel's whole catalogue back to that
#     date instead, e.g. to pull in years of back catalogue for a channel you
#     really want to mine for patterns. Leave it out for everything else.

channels:
<<<<<<< HEAD
  - url: "https://www.youtube.com/@BlackMensBeard/videos"
    niche: "hairloss"
  # Example of a deeper backfill for one specific channel:
  # - url: "https://www.youtube.com/@SomeChannel"
  #   niche: "hairloss"
  #   since: "2023-01-01"
=======
  - id: "UC7kKFKme90zilijSwYlvwRQ"
    name: "Black Men's Beard"
    niche: "hairloss"
>>>>>>> 17790e7f32d7ad358666bd636ccdee148550d17a

# Add the rest of your ~50-150 channels below, one per line, e.g.:
#  - url: "https://www.youtube.com/@SomeOtherChannel"
#    niche: "hairloss"
#  - url: "https://www.youtube.com/@AnEcomChannel/videos"
#    niche: "ecom"
